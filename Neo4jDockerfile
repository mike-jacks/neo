# Use the official Neo4j image from Docker Hub
FROM neo4j:latest

# Expose the ports for Neo4j
EXPOSE 7474 7687

# Define the volumes for data persistence
VOLUME /data

# Set the default command to run Neo4j
CMD ["neo4j"]
